package de.zwegen.zpaint.command.implementation

/** The two quarter turns offered by Zaint's transform controls. */
enum class ZaintRotationDirection { LEFT, RIGHT }

/** The two reflection axes offered by Zaint's transform controls. */
enum class ZaintMirrorDirection { HORIZONTAL, VERTICAL }
