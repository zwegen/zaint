# Zaint

Zaint ist ein Bildeditor für Android.

This folder is a compact, GitHub-ready source export. It contains the current
Android app source and required build files, but no Git history, release APKs,
signing material, local configuration, backups or test sources.

## Entwicklung

Debug-Build erstellen:

```bash
./gradlew :app:assembleDebug
```
